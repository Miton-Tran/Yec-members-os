const config = {
  siteName: process.env.SITE_NAME || "YEC MEMBERS",
  exampleEmail: process.env.EXAMPLE_EMAIL || "",
  examplePassword: process.env.EXAMPLE_PASSWORD || "",
  demoDomain: process.env.DEMO_DOMAIN || "",
};

export default config;
